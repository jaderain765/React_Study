import { styled } from '@mui/material/styles';
import Paper from '@mui/material/Paper';

export const Item = styled(Paper)(({ theme }) => ({
    padding: '10px',
    borderTopWidth: '3px',
    borderTopColor: '#0A3D67',
}));
